name: test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    name: Node
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: npm install
      - run: yarn build
      - run: yarn test
